(* Package version, generated from the (version ...) field of dune-project by
   a rule in lib/dune. This is the single source of truth: it is reported by
   [tree-md --version] and stamped into the manifest's "compiler" field. *)

val current : string
